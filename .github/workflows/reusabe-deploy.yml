name: Reusable Deploy workflow

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Print deploy step
        run: echo "Deploy step"    
